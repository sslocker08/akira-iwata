# 作品集の編集ガイド

> **岩田くんへ**: 何もインストールする必要はありません。
> 作者から届いた**編集用URL**をブラウザ（Safari でOK）で開くだけで、編集画面が使えます。
> 編集・保存・**本番公開まで全部この画面ひとつ**で完結します。

## 使い方

1. 作者にもらった**編集用URL**（`https://akira-iwata.web.app/admin.html#k=…` の形）を開きます。
   - 初回に開くと編集キーが自動でブラウザに記憶されます。以後は
     `https://akira-iwata.web.app/admin.html` をブックマークしておけばOKです。
   - **このURLは「編集の鍵」です。他の人に渡さないでください。**
2. 編集画面は**実際のサイトに近い縦長のプレビュー**になっています。上から下へスクロールしながら編集します。
   - **画像の入れ替え**: 画像を**ドラッグ**すれば、行の中・行の間・別の画像との差し替えまで、どこでもドラッグ&ドロップでできます。
     （全幅・全画面・屏風の画像も同じようにドラッグで差し替え可能）
   - **新しい画像の追加**: 画面のどこでも良いので、パソコンから画像ファイルをドラッグ&ドロップ（またはギャラリーの「＋」をクリックして選択）。
     自動でサイズ最適化され、画面上部に**「新しく追加した画像」の一時置き場**が出てきます。そこから好きな場所へドラッグして配置してください。
     - このとき**元の画像（原本）が自動で「ダウンロード」フォルダに保存されます**（`ex-041-原本.jpg` のような名前）。
       サイトには縮小版だけが載るので、**原本は消さずに取っておいてください**（印刷などで将来使うため）。
   - **行の移動**: 各ブロックの左上に出る「⠿ 移動」を**ドラッグ**すると、行ごと好きな位置へ動かせます。
   - **レイアウトの変更**: 各ブロックの「行（複数）／全幅／全画面／屏風」の選択欄で、その場でレイアウトを変更できます。
   - **画像の削除/説明文**: 画像を**クリック**すると、右側に説明文（alt）の編集と「完全に削除」が出ます。
   - **文章の修正**: 各セクションの見出し・サブラベル、表紙の氏名、プロフィールの学歴・スキルなどを直接書き換え。
   - **行の追加/削除**: 「＋ 行を追加」で新しい行を追加。各ブロックの「削除」でその行ごと削除。
   - **収蔵庫（ギャラリー）**: 一番下のセクションで、全画像の**追加・削除・並べ替え**（ドラッグ）ができます。
3. **保存** … 下書きとして保存します（何度でもOK。**本番サイトには反映されません**）。
   **プレビュー↗** … いま編集中の内容を、実際のサイトの見た目で確認できます。
4. **本番へ公開** … 保存済みの内容を実際のサイト（https://akira-iwata.web.app）に反映します。1〜2分で反映されます。

> 「保存」= 下書き更新（何度でもOK）。「本番へ公開」= 実際のサイトに反映。
> 途中でブラウザを閉じても、**保存した下書きは残っています**（次に開いたとき下書きから再開されます）。
> 保存していない変更は消えるので、区切りごとに「保存」を押してください。

### うまくいかないとき

- 「編集キーが無効か期限切れです」→ キーの有効期限（約1年）が切れています。作者に新しいURLをもらってください。
- 「ヘルパに接続できません」等ではなく読み込みが失敗する → ネット接続を確認してください（この画面はオンライン専用です）。
- 画像が追加できない → JPEG か PNG で試してください。

## 仕組み（技術メモ）

- **データの正本は GitHub リポジトリ**（`sslocker08/akira-iwata`）。編集画面はブラウザから GitHub API を直接読み書きする。
  端末側に Node.js・その他のインストールは一切不要（Safari 可）。
- `content/site.json` … 唯一の編集元(構成・文章・画像メタ)。
- **保存** = `draft` ブランチへコミット。**公開** = draft の内容を `main` へ反映し、draft を main に揃える。
- **main が更新されると GitHub Actions**（`.github/workflows/deploy.yml`）**が `tools/build_site.mjs` を実行して
  `index.html` / `assets/js/data.js` を再生成・コミットし、Firebase Hosting へデプロイする**。
- サイト生成ロジックの本体は `assets/admin/build-core.mjs`（単一ソース）。CI の build も編集画面の「プレビュー↗」も同じものを使う。
- **画像最適化はブラウザ内の Canvas** で行う（works ≤1800px 品質80 / thumbs ≤400px 品質72。旧 `sips` 依存を廃止）。
  画像原本はリポジトリに入れず、追加時に編集者の端末へ自動ダウンロードして手元保管とする。
- 編集キーは URL の `#k=…`（フラグメント＝サーバーに送信されない）で受け取り、ブラウザの localStorage に保存する。
- `admin.html` と `assets/admin/` は本番にホストされるが、キーが無い限り閲覧・書き込みとも何もできない
  （リポジトリ自体が public なので、画面やコードの露出は新たなリスクではない）。

## 公開の仕組みとセットアップ（作者向け・一度だけ）

1. **Firebase デプロイ用のサービスアカウントを Secret に登録**
   - [Firebase コンソール](https://console.firebase.google.com) → 対象プロジェクト → プロジェクトの設定 → サービスアカウント →
     「新しい秘密鍵の生成」で JSON キーをダウンロード。
   - GitHub リポジトリ → Settings → Secrets and variables → Actions → New repository secret →
     名前 `FIREBASE_SERVICE_ACCOUNT`、値に JSON の中身を貼り付け。
   - `.github/workflows/deploy.yml` の `projectId: akira-iwata` が実際の Firebase プロジェクトIDと一致しているか確認
     （違う場合はプロジェクトの設定に表示される ID に直す）。
2. **編集キー（fine-grained PAT）を発行して編集用URLを作る**
   - GitHub → Settings → Developer settings → Personal access tokens → **Fine-grained tokens** → Generate new token。
   - Repository access: **Only select repositories → `akira-iwata` のみ**。
   - Permissions: **Contents → Read and write** だけ。他は一切不要。
   - 期限は最長（1年）にし、生成されたトークンで編集用URLを組み立てて岩田くんに送る:
     `https://akira-iwata.web.app/admin.html#k=<トークン>`
3. **キーの失効・更新**
   - 無効化したいとき: 上記 Fine-grained tokens の画面からトークンを Revoke するだけ（即時に編集不能になる）。
   - 期限が切れたら新しいトークンで URL を作り直して送る（年1回）。
   - このキーでできるのは**このリポジトリの内容の読み書きのみ**。万一URLが漏れてもサイト内容の書き換えまでで、
     アカウントや他リポジトリには影響しない。履歴は git に残るので巻き戻しも可能。

## 手動で作り直したいとき（作者向け）

```
node tools/build_site.mjs   # site.json から index.html / data.js を再生成
```

通常は不要（main への push で CI が自動実行する）。旧ローカル編集ヘルパ（`tools/serve-admin.mjs`・`start.command`）は
ブラウザ完結化に伴い廃止した（必要なら git 履歴から復元できる）。
