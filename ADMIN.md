# 作品集の編集ガイド

このサイトは **`content/site.json`（構成・文章）** と **画像フォルダ** をもとに、
`tools/build_site.mjs` が `index.html` を自動生成する仕組みです。
編集は専用の**ローカル編集画面**から行います（プログラミング不要）。

## 必要なもの
- Mac（画像の最適化に標準の `sips` を使います）
- Node.js（`node -v` で確認。無ければ https://nodejs.org からインストール）

## 編集のはじめかた

1. ターミナルでこのフォルダに移動し、編集ヘルパを起動します。
   ```
   node tools/serve-admin.mjs
   ```
2. ブラウザで **http://localhost:4321/admin.html** を開きます。
3. 画面上で編集します。
   - **画像の移動**: サムネイルを**ドラッグ**して、行の中・行の間・別セクションへ動かせます。
   - **画像を追加**: 右の「画像を追加」で追加先セクションを選び、画像を選ぶ/ドロップ。
     自動でサイズ最適化され、そのセクションの末尾に新しい行として入ります。
   - **画像を削除/説明文**: 画像を**クリック**すると、説明文（alt）の編集、
     「配置から外す」（並びから外すが画像は残す）、「完全に削除」（画像ごと消す）ができます。
   - **文章の修正**: 各セクションの見出し・サブラベル、表紙の氏名、プロフィールの学歴・スキルなどを直接書き換え。
   - **行の追加/並べ替え**: 「＋ 行を追加」で横一列を追加。全幅・全画面・屏風の要素は ↑↓ で並べ替え。
4. **保存**ボタンで下書きを保存（`content/site.json` に書き込み、プレビューが更新されます）。
   「プレビュー↗」でその場の見た目を確認できます。
5. 世に出す準備ができたら **本番へ公開**。数分で本番サイトに反映されます。

> 「保存」= 手元の下書き更新（何度でもOK）。「本番へ公開」= 実際のサイトに反映。

## 仕組み（技術メモ）
- `content/site.json` … 唯一の編集元（構成・文章・画像メタ）。
- `tools/build_site.mjs` … site.json から `index.html` と `assets/js/data.js` を生成。
- `tools/serve-admin.mjs` … ローカル編集ヘルパ。保存・画像最適化（sips）・公開（`git push`）を担当。
  **GitHub のトークン等は一切保持しません**。公開はこの端末に設定済みの git 資格情報を使います。
- 画像原本は `image/<セクション>/` に置かれます（サイズが大きいためリポジトリには含めず手元のみ）。
  公開されるのは最適化済みの `img/works`（表示用）と `img/thumbs`（一覧用）です。

## 手動で作り直したいとき
```
node tools/build_site.mjs   # site.json から index.html / data.js を再生成
```
