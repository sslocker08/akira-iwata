# 作品集の編集ガイド

> **岩田くんへ**: このフォルダ一式が届いていれば、下の「必要なもの」から読み進めてください。
> ネットに繋がなくても、フォルダの中だけで編集・見た目の確認までは完結します
> （インストールに使う最初の2行だけネットが要ります）。
> **本番サイトに公開する**（世の中に表示されている https://akira-iwata.web.app を書き換える）操作だけは、
> 岩田くんに権限が渡っているかどうかで挙動が変わります。詳しくは「公開について」の章を参照してください。
>
> 画像で手順を見たい場合は `setup-guide/` フォルダの `00〜06` を順番に開いてください。

このサイトは **`content/site.json`（構成・文章）** と **画像フォルダ** をもとに、
`tools/build_site.mjs` が `index.html` を自動生成する仕組みです。
編集は専用の**ローカル編集画面**から行います（プログラミング不要）。

## 必要なもの
- Mac だけでOKです。**`start.command` をダブルクリックするだけ**で、Node.jsのインストール（初回のみ・自動）から
  編集画面を開くところまで、まとめて実行されます。
  - 初回、Node.jsが入っていない場合は自動でインストールが始まり、途中で**Macのパスワード入力**を求められます（インストール許可のためです）。ここだけネット接続が必要です。
  - 「開発元を確認できません」という警告が出た場合は、`start.command` を**右クリック→「開く」**を選んでください。

編集・プレビューはこれだけで動きます。本番公開用の Firebase ログインは
「公開について」の章を参照してください（岩田くんの場合、権限付与の状況次第です）。

## 編集のはじめかた

1. フォルダの中の **`start.command`** をダブルクリックします。
   （手動で起動したい場合は、ターミナルで `node tools/serve-admin.mjs` でも同じことができます）
2. 自動でブラウザが開き、**編集画面**（http://localhost:4321/admin.html）と**プレビュー**の2つのタブが表示されます。
3. 編集画面は**実際のサイトに近い縦長のプレビュー**になっています。上から下へスクロールしながら編集します。
   - **画像の入れ替え**: 画像を**ドラッグ**すれば、行の中・行の間・別の画像との差し替えまで、どこでもドラッグ&ドロップでできます。
     （全幅・全画面・屏風の画像も同じようにドラッグで差し替え可能）
   - **新しい画像の追加**: 画面をどこでも良いので、パソコンから画像ファイルをドラッグ&ドロップ（またはクリックして選択）。
     自動でサイズ最適化され、画面上部に**「新しく追加した画像」の一時置き場**が出てきます。そこから好きな場所へドラッグして配置してください。
   - **行の移動**: 各ブロックの左上に出る「⠿ 移動」を**ドラッグ**すると、行ごと好きな位置へ動かせます。
   - **レイアウトの変更**: 各ブロックの「行（複数）／全幅／全画面／屏風」の選択欄で、その場でレイアウトを変更できます。
     （管理画面ではどれも同じ見た目で表示されます。実際のサイトでの見え方が変わるだけです）
   - **画像の削除/説明文**: 画像を**クリック**すると、右側に説明文（alt）の編集と「完全に削除」が出ます。
   - **文章の修正**: 各セクションの見出し・サブラベル、表紙の氏名、プロフィールの学歴・スキルなどを直接書き換え。
   - **行の追加/削除**: 「＋ 行を追加」で新しい行を追加。各ブロックの「削除」でその行ごと削除。
   - **収蔵庫（ギャラリー）**: 一番下のセクションで、全画像の**追加・削除・並べ替え**（ドラッグ）ができます。
     サムネイルの大きさはスライダーで調整できます。
4. **保存**ボタンで下書きを保存（`content/site.json` に書き込み、プレビューが更新されます）。
   「プレビュー↗」でその場の見た目を確認できます。
5. 世に出す準備ができたら **本番へ公開（akira-iwata.web.app）**。1分ほどで本番サイトに反映されます
   （公開権限がまだ無い場合はエラーになります。下の「公開について」を参照）。

> 「保存」= 手元の下書き更新（何度でもOK）。「本番へ公開」= 実際のサイト（https://akira-iwata.web.app）に反映。

## 公開について（岩田くんへ）

このツールで**編集・プレビューする**のは誰でもすぐできますが、**実際のサイトに反映する**（「本番へ公開」ボタン）には、
Firebaseプロジェクトへの書き込み権限が必要です。次の2通りのどちらかになります。

**A. 権限をもらっている場合**（本人が権限を渡し、下記コマンドでログイン済みなら）
```
npm install -g firebase-tools
firebase login       # ブラウザが開くのでGoogleアカウントでログイン
```
これだけで、以後は画面の「本番へ公開」ボタンがそのまま反映されます。

**B. 権限がまだの場合**
「本番へ公開」ボタンはエラーになりますが、**編集・保存・プレビューは問題なく使えます**。
編集が終わったら、更新された次の2箇所をそのまま（zipなどで）送り返してください。
本人の環境で「本番へ公開」を押すだけで反映します。
```
content/site.json          … 文章・構成の変更
img/works/ と img/thumbs/  … 新しく追加した画像（追加していれば）
image/<セクション名>/       … 追加した画像の原本（追加していれば）
```

## 仕組み（技術メモ）
- `content/site.json` … 唯一の編集元（構成・文章・画像メタ）。
- `tools/build_site.mjs` … site.json から `index.html` と `assets/js/data.js` を生成。
- `tools/serve-admin.mjs` … ローカル編集ヘルパ。保存・画像最適化（sips）・公開（`firebase deploy`）を担当。
- 公開先は **Firebase Hosting**（`firebase.json` の設定により、`admin.html` / `tools/` / `content/` / 原本画像などは
  一切アップロードされません。公開されるのは表示に必要なファイルだけです）。
- 副次的に GitHub リポジトリへも変更を控えとして保存します（コードの変更履歴・バックアップ用途。
  公開自体は Firebase 側で完結するため、失敗しても本番反映には影響しません）。
- 画像原本は `image/<セクション>/` に置かれます（サイズが大きいためどちらのリポジトリにも含めず手元のみ）。
  公開されるのは最適化済みの `img/works`（表示用）と `img/thumbs`（一覧用）です。

## 手動で作り直したいとき
```
node tools/build_site.mjs   # site.json から index.html / data.js を再生成
```
