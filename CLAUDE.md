<!-- skills-reference-rule:v8 (managed by Skills 工房 — 自動付与・このマーカーは編集しない) -->
## Skills 工房の継続参照（自動付与ルール）

このセッションには **Skills 工房**（再利用可能なスキル群 `.claude/skills/`・JIT索引 `_reference/skills-index.json`）がスコープに含まれている。**制作の全工程で、関連スキルを一度きりでなく随時・継続的に参照すること。**

- **三原則（Fable思考トレース）を全工程で常駐させる**: ①主張は事実/推測を仕分けて確信度＋出典を添える ②着手前にゴール・制約・却下基準を確定する ③自分の出力に最強の反対を組み込む ④**自分の文脈内の過去主張を「確認済みの前提」として扱わない**——過去の自己発言は cache であって source ではない。時制語・数値・判定・帰属を再利用する時は元ソース（ファイル/`date`/URL/grep）から再グラウンディングし、未検証の主張には最初の1回から〔未検証〕を付ける（**全モデル対象**・正本は `fable-thought-trace-claim-audit` 自己汚染節）。正本は工房の `fable-thought-trace`（詳細: `fable-thought-trace-claim-audit`／`fable-thought-trace-dissent`）。Fable 級モデルには詳細クランプを積まず宣言のみ（④は宣言に含まれる＝全 tier）。
- **着手前・方針決定・実装・レビューの各局面**で `_reference/skills-index.json`（keywords/use-when/description/layer/path の機械可読索引）を引き、該当スキルへ**直接ジャンプ**する（4ホップ委任を避ける）。
- **0→1・「作って／クローンして」系**は `request-intake-router`（フロントドア）→ `executive-director` を入口に。会社機能（営業/経営/戦略）は各 director を入口に。
- **やりたいことが「ビルド依頼」なら、単発スキルで完結させず必ずワイヤーに乗せる**: 入口〜出荷〜成長までの全行程結線図は工房の `e2e-pipeline-atlas`（依頼アーキタイプ別ワイヤー＋分岐判定点）。JIT 索引で単発スキルに直行した後でも、それがビルドの一部だと気づいたらアトラスへ戻り所属ワイヤーを引いてから進める。
- **横断craft**（design / motion / quality / discoverability / generative-media 等）は該当マネージャー配下スキルを **draw**（再発明しない）。design / motion / visual-craft のデザイン系 craft は**デザイン工房（sslocker08/design）所有**＝両工房がスコープにあれば `sslocker08/design:_reference/design-index.json` を引いて該当 `DESIGN.md` へ直行する。
- **工房パイプラインと参照規約**: 知見リポジトリは **MyBrain（判断・統率）が Skills（方法＝手順・ナレッジ）／Design（表現＝デザイン知見・作例）／Sparks（アイデア＝気になるアイデアの種）を統率する**構成で、スコープにあるものを随時参照する。投入ソースの振り分けは4方向（手順→Skills／デザイン→Design／判断・自己認識→MyBrain／気になるアイデアの種→Sparks）。参照の書き方＝①知見ユニット間は `[[name]]`（第一選択）②リポジトリ内のファイルは相対パス③リポジトリ間のファイルは修飾形式 `sslocker08/<repo>:<path>`。改名・移動時は Skills 工房の `scripts/link-graph.py --fix-rename` で関連参照を一括修正（正本は工房の `cross-workshop-reference-scheme` スキル）。
- スキルの知見は会話に依存せず自己完結している。**引用したら出典スキル名を一言添える**（例: 「`web-performance-core-web-vitals` に従い…」）。
- **ターン完了通知（操作者プリファレンス）**: 「指示→完了（Stop）」の各ターン末尾で、`PushNotification` に**1行の完了要約**（何が終わったか・要判断なら何を）を渡してスマホへ通知する。自己ゲート式＝**Remote Control（モバイルアプリ）接続時のみ端末へ push・未接続なら無害な no-op**／**閲覧中はハーネスが自動で重複抑止**するため、離席時だけ届く。クラウドセッションでは macOS の `osascript` は使えず、この標準ツールが唯一の transport（コスト無料）。ルーチンの細かな進捗では鳴らさず、区切り（完了・要判断・長時間処理の終了）で1回。
- **【厳格・一切禁止】中国語表記の混入禁止**: あらゆる出力（チャット・要約・生成物・PR/コミット・**生成画像の中の文字**）で、中国語表記を一切使わない——①簡体字（图/说/东/对/实…）②繁体字（新字体へ寄せる）③中国語特有の語彙（视频→動画／图片→画像／提示词→プロンプト／组件→コンポーネント等）④中国語の句読点（文中「，」→「、」・書名号《》不使用）。**漢字は必ず日本の新字体**。中国語ソースを引用する時のみ引用符/コードブロックで隔離した最小限の原文＋日本語訳は可。**思考過程でも中国語原文を地のまま扱わず、読み込んだ瞬間に日本語/英語へ翻訳してから進める**。予防知識・罠字表・検出手段の正本は工房の `output-language-purity` スキル。
<!-- /skills-reference-rule:v8 -->
